
.. _rm-save-relational-connection:

============================================
Save a Relational Database Connection
============================================

.. default-domain:: mongodb

.. contents:: On this page
   :local:
   :backlinks: none
   :depth: 1
   :class: singlecol

You can save a new relational database connection from the Relational Migrator
:guilabel:`Connections` page, or when creating or modifying a sync job.


Steps
-----

.. tabs::

   .. tab:: Oracle
      :tabid: db-oracle

      .. procedure::
         :style:  normal

         .. step:: Add a connection.
            
            Under :guilabel:`Relational` click :guilabel:`Add connection`.

         .. step:: Select the database type.
            
            In the :guilabel:`Database type` drop-down, select :guilabel:`Oracle`.
            
         .. step:: To use a saved :ref:`connection strings <rm-connection-strings>` or input one directly:
   
            a. Above the :guilabel:`JDBC URI` field, enable the
               :guilabel:`Enter URI manually` toggle.

            #. Paste your connection string into the :guilabel:`JDBC URI`
               field.

         .. step:: If you don't have a saved connection string, create it by 
            entering the following information:

            .. include:: /includes/table-oracle-connection-fields.rst

         .. step:: Enter the :guilabel:`Username` and :guilabel:`Password` to 
            use for authentication.
               
            Checking :guilabel:`Save password` saves the password securely on 
            your machine. Relational Migrator automatically fills in the 
            :guilabel:`Password` field for the current project.

         .. step:: Specify a :guilabel:`Connection name` and optionally an 
            :guilabel:`Environment tag`.
               
            Environment tags color code and organize connections for 
            convenience. Leaving the drop-down empty is the same as selecting 
            the "None" tag.

         .. step:: Optionally, click :guilabel:`Test connection`.
               
         .. step:: Click :guilabel:`Save`.


   .. tab:: SQL Server
      :tabid: db-sql

      .. procedure::
         :style:  normal

         .. step:: Add a connection.
            
            Under :guilabel:`Relational` click :guilabel:`Add connection`.

         .. step:: Select the database type.
            
            In the :guilabel:`Database type` drop-down, select :guilabel:`SQL Server`.
            
         .. step:: To use a saved connection string or input one directly:
   
            a. Above the :guilabel:`JDBC URI` field, enable the
               :guilabel:`Enter URI manually` toggle.

            #. Paste your connection string into the :guilabel:`JDBC URI`
               field.
            
            For more information, see :ref:`connection strings <rm-connection-strings>`.

         .. step:: If you don't have a saved connection string, create it by 
            entering the following information:

            .. include:: /includes/table-sql-connection-fields.rst

         .. step:: Enter the :guilabel:`Username` and :guilabel:`Password` to 
            use for authentication.
               
            Checking :guilabel:`Save password` saves the password securely on 
            your machine. Relational Migrator automatically fills in the 
            :guilabel:`Password` field for the current project.

         .. step:: To connect using SSL:
            
            a. Toggle from :guilabel:`General` to :guilabel:`SSL`.
            
            #. Select an SSL mode.

         .. step:: Specify a :guilabel:`Connection name` and optionally an 
            :guilabel:`Environment tag`.
               
            Environment tags color code and organize connections for 
            convenience. Leaving the drop-down empty is the same as selecting 
            the "None" tag.

         .. step:: Optionally, click :guilabel:`Test connection`.
               
         .. step:: Click :guilabel:`Save`.

   .. tab:: MySQL
      :tabid: db-mysql

      .. procedure::
         :style:  normal

         .. step:: Add a connection.
            
            Under :guilabel:`Relational` click :guilabel:`Add connection`.

         .. step:: Select the database type.
            
            In the :guilabel:`Database type` drop-down, select :guilabel:`MySQL`.
            
         .. step:: To use a saved connection string or input one directly:
   
            a. Above the :guilabel:`JDBC URI` field, enable the
               :guilabel:`Enter URI manually` toggle.

            #. Paste your connection string into the :guilabel:`JDBC URI`
               field.
            
            For more information, see :ref:`connection strings <rm-connection-strings>`.

         .. step:: If you don't have a saved connection string, create it by 
            entering the following information:

            .. include:: /includes/table-mysql-connection-fields.rst

         .. step:: Enter the :guilabel:`Username` and :guilabel:`Password` to 
            use for authentication.
               
            Checking :guilabel:`Save password` saves the password securely on 
            your machine. Relational Migrator automatically fills in the 
            :guilabel:`Password` field for the current project.

         .. step:: To connect using SSL:
            
            a. Toggle from :guilabel:`General` to :guilabel:`SSL`.
            
            #. Select an SSL mode.

         .. step:: Specify a :guilabel:`Connection name` and optionally an 
            :guilabel:`Environment tag`.
               
            Environment tags color code and organize connections for 
            convenience. Leaving the drop-down empty is the same as selecting 
            the "None" tag.

         .. step:: Optionally, click :guilabel:`Test connection`.
               
         .. step:: Click :guilabel:`Save`.

   .. tab:: PostgreSQL
      :tabid: db-postgresql

      .. procedure::
         :style:  normal

         .. step:: Add a connection.
            
            Under :guilabel:`Relational` click :guilabel:`Add connection`.

         .. step:: Select the database type.
            
            In the :guilabel:`Database type` drop-down, select :guilabel:`PostgreSQL`.
            
         .. step:: To use a saved connection string or input one directly:
   
            a. Above the :guilabel:`JDBC URI` field, enable the
               :guilabel:`Enter URI manually` toggle.

            #. Paste your connection string into the :guilabel:`JDBC URI`
               field.
            
            For more information, see :ref:`connection strings <rm-connection-strings>`.

         .. step:: If you don't have a saved connection string, create it by 
            entering the following information:

            .. include:: /includes/table-postgresql-connection-fields.rst

         .. step:: Enter the :guilabel:`Username` and :guilabel:`Password` to 
            use for authentication.
               
            Checking :guilabel:`Save password` saves the password securely on 
            your machine. Relational Migrator automatically fills in the 
            :guilabel:`Password` field for the current project.

         .. step:: To connect using SSL:
            
            a. Toggle from :guilabel:`General` to :guilabel:`SSL`.
            
            #. Select an SSL mode.

         .. step:: Specify a :guilabel:`Connection name` and optionally an 
            :guilabel:`Environment tag`.
               
            Environment tags color code and organize connections for 
            convenience. Leaving the drop-down empty is the same as selecting 
            the "None" tag.

         .. step:: Optionally, click :guilabel:`Test connection`.
               
         .. step:: Click :guilabel:`Save`.

   .. tab:: Db2
      :tabid: db-db2

      .. procedure::
         :style:  normal

         .. step:: Add a connection.
            
            Under :guilabel:`Relational` click :guilabel:`Add connection`.

         .. step:: Select the database type.
            
            In the :guilabel:`Database type` drop-down, select :guilabel:`Db2`.
            
         .. step:: To use a saved connection string or input one directly:
   
            a. Above the :guilabel:`JDBC URI` field, enable the
               :guilabel:`Enter URI manually` toggle.

            #. Paste your connection string into the :guilabel:`JDBC URI`
               field.
            
            For more information, see :ref:`connection strings <rm-connection-strings>`.

         .. step:: If you don't have a saved connection string, create it by 
            entering the following information:

            .. include:: /includes/table-db2-connection-fields.rst

         .. step:: Enter the :guilabel:`Username` and :guilabel:`Password` to 
            use for authentication.
               
            Checking :guilabel:`Save password` saves the password securely on 
            your machine. Relational Migrator automatically fills in the 
            :guilabel:`Password` field for the current project.

         .. step:: Specify a :guilabel:`Connection name` and optionally an 
            :guilabel:`Environment tag`.
               
            Environment tags color code and organize connections for 
            convenience. Leaving the drop-down empty is the same as selecting 
            the "None" tag.

         .. step:: Optionally, click :guilabel:`Test connection`.
               
         .. step:: Click :guilabel:`Save`.

   .. tab:: Sybase
      :tabid: db-sybase

      .. procedure::
         :style:  normal

         .. step:: Add a connection.
            
            Under :guilabel:`Relational` click :guilabel:`Add connection`.

         .. step:: Select the database type.
            
            In the :guilabel:`Database type` drop-down, select :guilabel:`Sybase ASE`.
            
         .. step:: To use a saved :ref:`connection strings <rm-connection-strings>` or input one directly:
   
            a. Above the :guilabel:`JDBC URI` field, enable the
               :guilabel:`Enter URI manually` toggle.

            #. Paste your connection string into the :guilabel:`JDBC URI`
               field.

         .. step:: If you don't have a saved connection string, create it by 
            entering the following information:

            .. include:: /includes/table-sybase-connection-fields.rst

         .. step:: Enter the :guilabel:`Username` and :guilabel:`Password` to 
            use for authentication.
               
            Checking :guilabel:`Save password` saves the password securely on 
            your machine. Relational Migrator automatically fills in the 
            :guilabel:`Password` field for the current project.

         .. step:: Specify a :guilabel:`Connection name` and optionally an 
            :guilabel:`Environment tag`.
               
            Environment tags color code and organize connections for 
            convenience. Leaving the drop-down empty is the same as selecting 
            the "None" tag.

         .. step:: Optionally, click :guilabel:`Test connection`.
               
         .. step:: Click :guilabel:`Save`.